# Hank的工作台 · 部署与双端同步指南

工程已在本机初始化为 Git 仓库（分支 `main`，已提交）。
双端同步必须走「EdgeOne Pages 从 Git 仓库导入」——拖文件夹上传跑不了同步后端的依赖，会 404。

---

## 阶段一：把工程推到 GitHub（用你的账号）

1. 打开 github.com，登录或注册。
2. 点右上角 **＋ → New repository**，仓库名随便起（如 `hank-dashboard`）：
   - Public / Private 都行（EdgeOne 授权后能读）；
   - **不要**勾选 "Add a README" / ".gitignore"（保持空仓库）。
3. 把工程推上去，二选一：

   **法 A · GitHub Desktop（最省事，推荐）**
   - 安装 GitHub Desktop 并登录同一账号；
   - File → Add Local Repository → 选本机这个 `Hank的工作台` 文件夹；
   - Publish repository → 选刚建的仓库 → Push。

   **法 B · 命令行**（在本机终端 `cd` 进文件夹后执行）
   ```
   git remote add origin https://github.com/你的用户名/hank-dashboard.git
   git push -u origin main
   ```
   首次会要 GitHub 个人令牌（Settings → Developer settings → Personal access tokens，勾 `repo`）。

> 推完把仓库地址发我，或截图给我看。

---

## 阶段二：EdgeOne Pages 从 Git 导入

1. 搜「EdgeOne Pages」进腾讯云控制台，微信扫码登录。**首次要实名认证**（身份证+人脸，约 5 分钟，绕不开）。
2. 新建项目 → 选 **「从 Git 仓库导入」** → 授权连接 GitHub → 选 `hank-dashboard` 仓库。
3. 构建设置（控制台按钮名可能变，按「入口特征」找）：
   - 框架预设：选「无 / Other / 静态」类；
   - **构建命令：留空**（平台导入 Git 时会自动 `npm install` 装同步依赖）；
   - **输出目录：设为根目录 `/`**（因为 `index.html` 在根，不是子目录）；
   - 根目录：`/`。
4. 点部署，等构建完成，拿到 `xxx.edgeone.app` 链接 —— 这就是你的工作台，永久有效。

⚠️ 控制台界面常改版。哪一步对不上，**截图发我**，我按截图指路，不凭记忆猜。

---

## 阶段三：开启双端同步（在 app 里做）

1. 浏览器打开上面的链接 → 侧栏 **⚙️ 设置 → 双端同步 → 开启**；
2. 设一个**同步账号（用户名）** + **密码**：
   - 密码记住！忘了进不去，没有找回功能；
   - 建议先在设置里点一次「导出备份」，把文件转发到微信文件传输助手存着。
3. 手机和电脑用**同一账号密码**登录，数据自动对齐（打开时/切回前台/手动「立即同步」才拉取，不后台轮询）。
4. 刚在另一台改的东西，这台最多等约 1 分钟才看得到，等不及就点「立即同步」。

---

## 阶段四：加到手机桌面（全屏 App 体验）

- **iOS（Safari，不能用微信打开）**：打开链接 → 底部「分享」→ 下滑找「添加到主屏幕」→ 改名 → 添加。
- **安卓（Chrome）**：打开链接 → 右上角「···」→「添加到主屏幕 / 安装应用」。

> 微信内置浏览器没有「添加到主屏幕」，这是最高频卡点。手机上先在浏览器打开链接。

---

## 卡住时的退路

- 实在不想弄 GitHub / 实名：可降级成**拖文件夹上传（L1）**——能拿到链接、加到桌面，只是两台设备各记各的、不互通。随时能改天再升级同步。
- 推上去后打开 404：多半拖了单个文件而不是整个文件夹，或 `index.html` 不在根。
- 同步报「找不到 @edgeone/pages-blob」：说明没走 Git 导入或 `package.json` 不在根——回到阶段二。
- 任一环节卡超过 2 次：先降级 L1 拿到能用的，别耗着。

## 费用

个人用量（一天开几次、两台设备）每月约千次函数调用，免费额度百万级。建议在腾讯云设个费用告警；谨慎就别开「后付费」，最坏是停服不是扣钱，且本地数据还在、工作台照常用。
