# Hank的工作台 · 部署与双端同步指南（GitHub Pages + Gist 版）

工程已在本机初始化为 Git 仓库（分支 `main`），并推到了 GitHub 公开仓库 `hank920776329/hank-dashboard`。

同步后端用的是 **GitHub Gist + AES 加密**：数据存进一个**私有 Gist**（不是主仓库，不污染提交历史），内容用你设的「同步密码」加密，没密码读不了。不需要任何第三方服务器，也没有 EdgeOne 那种 401 预览网关。手机和电脑填同一套令牌 + 密码即自动对齐。

---

## 阶段一：GitHub Pages 托管（拿到公开网址）

> 公开仓库的 Pages 站点**任何人都能直接打开、无需登录**，正好避开网关。

1. 打开 `https://github.com/hank920776329/hank-dashboard` → 顶部 **Settings**（设置）。
2. 左侧 **Pages**（页面）→ Source（来源）选：
   - Branch：**main**
   - 目录：**/ (root)**（根目录，因为 index.html 在根）
   - 点 **Save**。
3. 等 1–2 分钟，页面会显示 `https://hank920776329.github.io/hank-dashboard/` —— 这就是你的工作台永久网址，**手机电脑都能直接打开、无登录门槛**。
4. 把这个网址发我，或自己先打开验证（应看到 Hank的工作台，不是 401）。

> 若想更短/好记的网址，可之后在 Pages 里绑自定义域名（需自有域名 + ICP 备案），不急。

---

## 阶段二：开启双端同步（在 app 里做）

1. 浏览器打开上面的网址 → 侧栏 **⚙️ 设置 → 双端同步 → 开启**；
2. 填两项：
   - **个人访问令牌 PAT**：`ghp_...` 开头的那个（需在 GitHub 勾 **`gist`** 权限；可新建一个**只勾 gist** 的令牌专用于同步，比用推代码的 repo 令牌更安全）。存在本机浏览器里，用于读写你的私有 Gist。
   - **同步密码**：自己设一个（至少 8 位）——**用来 AES 加密你存在 Gist 里的数据**，忘了无法恢复。
3. 点「开启同步」。**第一台**设备会自动创建一个私有 Gist 并把当前数据加密传上去；**第二台**设备要连上同一个 Gist 才能互通，有两种方式：
   - **方式 A（推荐·最稳）**：第一台开启后，设置页会显示「Gist：`<一串ID>`」，把这串 ID 抄下来；第二台开启时，填**同一个 PAT + 同一个同步密码**，并在「已有 Gist ID（可选）」框里粘贴这串 ID → 开启。两端立刻共用同一个 Gist，数据互通。
   - **方式 B（自动）**：第二台直接填同一个 PAT + 密码开启，app 会自动按文件名 `hank-data.json` 在 Gist 列表里找已存在的那个并复用。但若你之前误开过多个 Gist，自动找可能连错，所以优先用方式 A。
   - 开启时 app 会先拉对方的已有数据合并，再写回，**不会覆盖另一方的内容**。
   - 改完会自动同步（防抖 4 秒）；切回前台 / 联网 / 点「立即同步」也会拉取。
4. 之后在 GitHub 的 Gist 列表里能看到一个名为 `hank-data.json` 的文件——那是加密后的同步数据，**明文也读不了**，放心。

> 安全提示：PAT 存在你本机浏览器，能读写你的 Gist。若担心，用只勾 `gist` 的专用令牌。同步密码是 AES 密钥，GitHub 平台也看不到明文。

---

## 阶段三：加到手机桌面（全屏 App 体验）

- **iOS（Safari，不能用微信打开）**：打开链接 → 底部「分享」→ 下滑找「添加到主屏幕」→ 改名 → 添加。
- **安卓（Chrome）**：打开链接 → 右上角「···」→「添加到主屏幕 / 安装应用」。

> 微信内置浏览器没有「添加到主屏幕」，这是最高频卡点。手机上先在浏览器打开链接。

---

## 本地验收 / 日常

- 加一条待办 → 刷新页面数据还在（localStorage）；
- 手机和电脑各加一条 → 两边都点「立即同步」→ 应互相对齐；
- **换手机 / 清浏览器前**，设置里点「导出备份」存一份 JSON（转发微信文件传输助手）。

---

## 卡住时的退路

- 打开 Pages 网址是 404：Settings → Pages 来源可能没保存，回去确认 Branch=main、目录=/ (root)。
- 同步报「Gist 读取失败 / 写入失败 401」：PAT 失效或没勾 `gist` 权限，重新生成一个带 `gist` 的令牌填进去。
- 同步报「创建失败 422」：一般是令牌权限不够或网络问题，换网络重试；确认令牌有 `gist` 权限。
- 忘记同步密码：本地数据还在、可继续用；云端那份打不开。用「导出备份」保住本地，再「关闭同步」重开设新密码（会新建一个 Gist 覆盖）。
- 想彻底不要同步：设置里「关闭同步」，纯本地使用，不受影响。

## 费用

GitHub Pages + Gist：个人用量完全免费（GitHub 公开仓库 + 5000 次 API/小时，远超日用）。
